# Releases

The complete GTraj Analyzer software package will be distributed through GitHub Releases.

Planned first public release:

`GTraj-Analyzer-v1.0`

The release archive will contain the application source, analysis modules, interface templates, documentation, and minimal test files. Large preconfigured environments will be hosted separately and linked from `environment/README.md`.
